# Auto-generated by AI Factory — replace with your real compose file.
version: "3.9"
services:
  api:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      - APP_PORT=8000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 5s
      timeout: 5s
      retries: 12
